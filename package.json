{
  "name": "tarigym-poster-competition",
  "version": "1.0.0",
  "description": "TARIGYM Poster Competition Management System",
  "scripts": {
    "start": "node server/server.js",
    "build": "npm install --prefix server && npm install --prefix client && npm run build --prefix client && cp -r client/build server/public && echo 'Build completed and copied to server/public'",
    "postinstall": "npm install --prefix server"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "author": "Ram Narayan",
  "license": "ISC"
}
