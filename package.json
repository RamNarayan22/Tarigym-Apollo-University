{
  "name": "tarigym-poster-competition",
  "version": "1.0.0",
  "description": "TARIGYM Poster Competition Management System",
  "scripts": {
    "start": "node server/server.js",
    "build": "npm install --prefix server && npm install --prefix client && npm run build --prefix client && echo 'Build folder contents:' && ls -la client/build || echo 'Build folder not found'",
    "postinstall": "npm install --prefix server"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "author": "",
  "license": "ISC"
}
